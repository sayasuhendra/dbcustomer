<?php

class ContactvendorsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('contactvendors')->truncate();

		$contactvendors = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('contactvendors')->insert($contactvendors);
	}

}
