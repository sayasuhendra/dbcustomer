<?php

class AmatsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('amats')->truncate();

		$amats = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('amats')->insert($amats);
	}

}
