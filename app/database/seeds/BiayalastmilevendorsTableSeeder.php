<?php

class BiayalastmilevendorsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('biayalastmilevendors')->truncate();

		$biayalastmilevendors = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('biayalastmilevendors')->insert($biayalastmilevendors);
	}

}
